<%= form_with(model: write_in_candidate) do |form| %>
  <% if write_in_candidate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(write_in_candidate.errors.count, "error") %> prohibited this write in candidate from being saved:</h2>

      <ul>
        <% write_in_candidate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :candidate_name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <br/>
  <div>
    <%= form.submit "Vote" %>
  </div>
<% end %>
