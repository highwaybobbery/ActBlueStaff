<!DOCTYPE html>
<html>
  <head>
    <title>ActBlueStaffChallenge</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div style="clear:both; height: 20px;">
    <div style="float:left"><a href="/">VOTE.WEBSITE</a></div>
    <div style="float:right; margin-left: 10px;">
      <% if current_user.present? %>
        <%= button_to 'Sign Out', '/sessions/destroy', method: :delete %>
      <% elsif ! current_page?(controller: 'sessions', action: 'new') %>
        <%= button_to 'Sign In to Vote', '/sessions/new', method: :get %>
      <% end %>
    </div>
    <div style="float:right">
      <%= current_user&.email %>
    </div>
  </div>
  <hr/>
    <%= yield %>
  </body>
</html>
